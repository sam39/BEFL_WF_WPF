<Window x:Class="WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <DataGrid AutoGenerateColumns="False" x:Name="empGrid" HorizontalAlignment="Left" Margin="10,79,0,0" VerticalAlignment="Top" Height="231" Width="497">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding LastName}" Header="Фамилия" Width="100" />
                <DataGridTextColumn Binding="{Binding Name}" Header="Имя" Width="100" />
                <DataGridTextColumn Binding="{Binding SName}" Header="Отчество" Width="100" />
                <DataGridComboBoxColumn SelectedValueBinding="{Binding DepId}" DisplayMemberPath="Dep.Name" Header="Должность" Width="100" />
            </DataGrid.Columns>
            
        </DataGrid> 

    </Grid>
</Window>
