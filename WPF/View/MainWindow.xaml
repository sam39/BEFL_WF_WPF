<Window x:Class="WPF.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF.View"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="600">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Hidden">
            <StackPanel>
                <Button Content="Сотрудники" Click="Button_Click" />
                <Button Content="Компьютеры" Click="Button_Click_1" />
                <Button Content="Мониторы" />
                <Button Content="Принтеры" />
                <Button x:Name="btnDeps" Content="Отделы" Click="btnDeps_Click" />
                <Button x:Name="btnPoss" Content="Должности" Click="btnPoss_Click" />                
            </StackPanel>
        </ScrollViewer>

        <Frame x:Name="MainFrame" Grid.Column="1" Background="#CCCCCC"/>
    </Grid>
</Window>
